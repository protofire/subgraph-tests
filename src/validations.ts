// if (!infuraEndpoint) {
//   throw Error("You must enter INFURA access token as first arg");
// }

// if (!contractAddress) {
//   throw Error("You must enter contract address as second arg");
// }

export function sum(a: number, b: number): number {
    return a + b;
}